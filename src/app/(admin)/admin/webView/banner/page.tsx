import ListManager from "@adminShared/ListManager"

const Banner = () => {
  return (
    <ListManager
      title="배너 설정"
      placeholder="새로운 배너"
      buttonText="배너 등록"
    />
  )
}

export default Banner