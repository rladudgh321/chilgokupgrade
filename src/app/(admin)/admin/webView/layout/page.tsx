import ListManager from "@adminShared/ListManager"

const Layout = () => {
  return (
    <ListManager
      title="레이아웃 설정"
      placeholder="새로운 레이아웃"
      buttonText="레이아웃 등록"
    />
  )
}

export default Layout