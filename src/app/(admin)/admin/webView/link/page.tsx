import ListManager from "@adminShared/ListManager"

const OutLink = () => {
  return (
    <ListManager
      title="외부링크 설정"
      placeholder="새로운 외부링크"
      buttonText="외부링크 등록"
    />
  )
}

export default OutLink